Opens the session summary overlay — the same thing `/summary` shows. Press again, or Esc, to close.

Shares its slot with the tool-output overlay (Ctrl+O) — opening one closes the other. PgUp/PgDn scroll the overlay while it's open.

## Examples

    Ctrl+S
