Clears the chat view on screen. The session itself — its record, summary, and the agent's context window — is untouched.

Use it when the scrollback is cluttered but you want to keep going in the same conversation; the agent still remembers everything said before you cleared. This is different from `/reset`, which actually ends the session, and from `/wipe`, which deletes saved state from disk.

## Examples

    /clear
