Ask about behaviour the code DECIDES: the value a member returns, the condition that selects one
branch over another, the order in which state is mutated, the invariant the code preserves without
stating it.

An answer to a good question here contains a value, a name, an order or a condition.
