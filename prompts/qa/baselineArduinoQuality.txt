This is an Arduino sketch, so apply these embedded-specific recommendations as SOFT guidance, not rigid law — raise them only when clearly ignored, never invent work the user did not ask for: pin numbers should be named constants (`const int LED_PIN = 13;`), not magic numbers scattered through the file; a `delay()` inside `loop()` should be there because the described behavior is genuinely meant to block, not as a lazy stand-in for timing that should use `millis()` so the loop stays responsive; `Serial.begin(...)`'s baud rate should match whatever the code comments, README, or a paired `Serial.begin` in an accompanying tool/monitor config expect, since a mismatch there produces garbled output with no compile error to catch it.
