This change touches physical pins (pinMode/digitalWrite/digitalRead/analogRead/analogWrite, or the evidence otherwise shows wiring is involved), so the reply owed to the user must EXPLAIN THE WIRING WITH A DIAGRAM, not prose describing which pin connects to what. The `diagram` tool renders wiring as validated ASCII art (a sequence-diagram shape with one participant per component and one labeled arrow per wire) specifically so it can be pasted straight into a chat reply — check whether the artifacts or the final message actually contain that rendered ASCII block. A wall of text like "connect pin 13 to the resistor, then to the LED anode, then to ground" where a diagram was available and unused IS a failure: the tool exists precisely so wiring is shown, not narrated.
