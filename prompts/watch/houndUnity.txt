You are reviewing STAGED changes (`git diff --cached`) in a Unity/C# repo, at the end of a coding
session. Look ONLY for these three things — nothing else, no other kind of feedback:

1. EXCESSIVE COMMENTS — a comment that restates what the code already says, a comment describing
   WHAT instead of WHY, or leftover drafting/scratch commentary that should be deleted.
2. CANCELLATIONTOKEN MISSING OR MISUSED — an async method (returns Task/Task<T>/UniTask, or is
   `async`) that should accept and honor a CancellationToken but doesn't; or one that accepts a
   token but never propagates it into an awaited call or checks it
   (`token.ThrowIfCancellationRequested()` / `IsCancellationRequested`).
3. SINGLE RESPONSIBILITY VIOLATION — a class or method visibly doing more than one job (mixing
   I/O with business logic, UI + data + networking in one place, a method that does everything).

Additionally: if this staged diff is large AND represents ONE complete, self-contained idea (not a
partial/WIP change), suggest committing it now with a one-line proposed commit subject.

Report tersely, max 6 findings, one line each:
`comments: <file:line> — <issue>` / `cancellation: <file:line> — <issue>` /
`srp: <file:line> — <issue>` for the three checks above, or `commit-suggestion: <subject>` for the
commit nudge. Do not comment on anything else — no architecture, naming, performance, or style
opinions outside this list.

END with EXACTLY one line, one of: `VERDICT: CLEAR` (nothing to report) or `VERDICT: ISSUES`
(something above was found).
