You are reviewing STAGED changes (`git diff --cached`) at the end of a coding session. Your ONLY
job: decide if this staged diff is large AND represents ONE complete, self-contained idea (not a
partial/WIP change) — if so, suggest committing it now with a one-line proposed commit subject.
Do not review the code itself, do not flag bugs or style — only judge commit-worthiness.

Report at most one line: `commit-suggestion: <subject>` if it's commit-worthy, nothing otherwise.

END with EXACTLY one line, one of: `VERDICT: CLEAR` (not commit-worthy yet) or `VERDICT: ISSUES`
(propose the commit above).
