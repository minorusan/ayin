Write the STORY of this feature, in plain flowing prose — the way you'd explain it out loud to a
teammate who just joined and has never seen it, not a structured report. No markdown `##` headings, no
bullet-pointed sections: a handful of short paragraphs that read like one continuous explanation.
Every fact must trace back to something in the EVIDENCE below; where the evidence is thin or silent,
say so plainly, in prose, rather than filling the gap with a plausible-sounding guess.

FEATURE ASKED ABOUT: {{FEATURE}}

WHAT THE CODE ACTUALLY DOES (from a real investigation of the repo):
{{EXPLORE_FINDINGS}}

{{HISTORY_EVIDENCE}}

{{JIRA_BLOCK}}

Cover these beats, in this order, as prose paragraphs (not headings) — each one flows into the next:

1. WHAT IT IS AND WHO BUILT IT. Open with what this feature is, in one sentence. Then its origin: who
   developed it mainly (the AUTHORSHIP evidence above — name whoever actually holds the most commits,
   never a guess), and roughly when it was introduced. For the origin date, READ THE PER-FILE DATE
   RANGES IN CHURN BY FILE FIRST — do not just grab the oldest date anywhere in the commit list. If one
   file's range reaches much further back than the rest, that file is very likely a shared hub (an
   installer, a central service) the feature was later wired into, not proof the feature itself is that
   old; its distant past belongs to whatever else touched it over the years, not to this feature. Base
   the origin date on files whose OWN range is closely scoped to when the bulk of the listed churn
   happened — or a resolved Jira ticket's date/reporter if one was found. If authorship or a trustworthy
   origin date can't be recovered this way, say so plainly instead of inventing a name or date.

2. LIFECYCLE AND BUGS. What has happened to it since — has it been stable, or did it go through periods
   of churn and fixes? Name the bugfix-looking commits and what they were about, and name the
   file(s)/area(s) that churned the most and why that's worth knowing. If the evidence shows no visible
   bug history, say that plainly — don't manufacture a "had issues" narrative the commits don't support.

3. WHAT IT'S MADE OF. Walk through its composition, grounded in the explore findings — roughly how many
   files/modules it spans and what each significant one is responsible for (this file owns the UI, that
   one owns the state machine, this one talks to the backend, etc.). Name real files and functions, not
   generic descriptions.

4. HOW IT'S WIRED UP. How it gets initialized or registered (a DI installer, a startup hook, a config
   entry point — whatever the evidence shows), what it depends on (other systems, services, config
   values), and anything else about its setup someone touching it for the first time should know.

Close with one or two sentences on the single thing worth knowing before touching this feature.

Tone: plain, evidence-first, conversational — like a colleague telling the story, not a template being
filled in. A confident-sounding paragraph with nothing behind it is worse than an honest "the evidence
here is thin."
