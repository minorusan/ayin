Store the OpenAI API key (verified against OpenAI before saving), optionally with a model name. Call with no arguments to report whether a key is configured. Does not switch ayin to OpenAI — /model openai does that.
