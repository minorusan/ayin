Look up a common Arduino/electronics component (~30 starter-kit parts: LEDs, buttons, servos, sensors, displays, drivers, ICs) in a reference catalog — what it does and exactly which leg wires to which pin. Prefer this over recalling component facts from memory. NOT project-specific: it never knows what a particular sketch actually uses, only general facts about the part itself.
