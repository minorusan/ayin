Ask what a previous `ayin indulge` run already answered ABOUT THIS REPOSITORY — its questions, answers and the code they cite. It holds only what indulge was pointed at in this working tree: it is not documentation, and it knows nothing about ayin, its tools or anything outside this repo — use ayin_help for those. Consult it before investigating something from scratch; the answer may already exist with citations. A miss is not evidence about the subject, only that nothing covering it was indexed, so it names the domains it does hold and the nearest questions it has. Returns nothing at all if no corpus has been built here.
