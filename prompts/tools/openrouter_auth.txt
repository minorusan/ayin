Store the OpenRouter API key, verified against OpenRouter before saving, optionally with a model id. Reports how many of its models accept tools and how many of those cost nothing, plus the rate limit this key is actually on. Call with no arguments for status. It does not switch ayin to OpenRouter — /model openrouter does that, and bare /model then lists the models to pick from, free ones first.
