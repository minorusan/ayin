Write content to a file, creating parent directories if needed. Use for new files or complete rewrites — overwriting an existing file replaces all of it, so regenerating a large file from memory risks silently dropping content; use str_replace instead to edit part of an existing file.
