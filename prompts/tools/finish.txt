End the task and report. This is the only thing that ends a task: a reply with no tool call is treated as work in progress and you will be asked to continue. Describing what you are about to do is not doing it. Call finish(summary="what you did or found") the moment the work is done — someone who did not watch the session should understand the outcome from the summary alone, so state what you changed or what you concluded. Add cause="file, function, mechanism" when you have identified the problem and are deliberately NOT fixing it; that is a complete answer, not a failure. Do not invent further work to avoid calling this, and do not call it with an empty summary — it will be refused.
