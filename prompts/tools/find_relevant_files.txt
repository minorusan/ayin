Ask which files a task would touch, and get back a verified list with a note on each. Hand it the task in plain words — "add a retry to the uploader", "where does the session id get set" — and an agent with search tools reads the tree and reports the files that matter and why. Every path it names is checked to exist before you see it. Use this before editing when you do not already know which files are involved.
